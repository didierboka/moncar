import 'package:flutter/material.dart';


void main() {
  runApp(const MyApp());
}


class MonCarApplication extends StatelessWidget {

  const MonCarApplication({Key? key}) : super(key: key);

  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
